apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: social-media-analysis
  name: social-media-analysis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: social-media-analysis
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  revisionHistoryLimit: 5
  template:
    metadata:
      labels:
        app: social-media-analysis
    spec:
      containers:
      - image: beaware/social-media-analysis
        imagePullPolicy: Always
        name: social-media-analysis
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
